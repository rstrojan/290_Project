
<fieldset class="tickets">
	<legend>Departure Tickets</legend>
	<ul = class="departResult">
		{{#with dataList}}
		<li><span>From: </span><span id="city_input">{{this.city_input}}</span></li>
		<li><span>To: </span><span>Kholinar</span></li>
		<li><span>Departure: </span><span id="departure">{{this.departure}}</span></li>
		<li><span>Number of seats: </span><span id="travelerNum">{{this.travelerNum}}</span></li>
		<li><img id="barcode" src="/images/barcode.jpg" /></li>
	</ul>
		<legend>Return Tickets</legend>
	<ul = class="returnResult">
		<li><span>From: </span><span>Kholinar</span></li>
		<li><span>To: </span><span id="city_input">{{this.city_input}}</span></li>
		<li><span>Departure: </span><span id="departure">{{this.departure}}</span></li>
		<li><span>Number of seats: </span><span id="travelerNum">{{this.travelerNum}}</span></li>
		<li><img id="barcode" src="/images/barcode.jpg" /></li>
	</ul>
	{{/dataList}}
</fieldset>


<ul>
  {{#each dataList}}
    <li>{{this.name}}: {{this.value}}
  {{/each}}
</ul>